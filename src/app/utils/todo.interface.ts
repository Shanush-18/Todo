export interface AppRoutesModel {
    path: any;
    title: string;
}

export interface TaskModel {
    id: number;
    title: string;
    status:string;
    startDate: string;
    endDate: string;
    description: string;
}